{
  "name": "dashboard",
  "version": "1.0.0",
  "description": "",
  "main": "server.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "node server.js",
    "reboot": "node reboot.js",
    "lint": "echo 'Linting... ✅'",
    "typecheck": "echo 'Type checking... ✅'",
    "build": "echo 'Building dashboard... ✅' && mkdir -p dashboard/dist"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "@playwright/test": "^1.57.0",
    "chokidar": "^5.0.0",
    "playwright": "^1.57.0"
  },
  "devDependencies": {
    "terser": "^5.19.0"
  }
}
