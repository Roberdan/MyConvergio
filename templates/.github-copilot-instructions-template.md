# GitHub Copilot Instructions Template

Template for creating `.github/copilot-instructions.md` in your project. Customize sections below.

## Project Overview

**Name:** [Your Project Name]  
**Description:** [Brief description]  
**Languages:** [e.g., TypeScript, Python, Go]

## Path-Specific Rules

**`src/`** - Main source code
- Max 250 lines per file
- Naming: [e.g., kebab-case for files, PascalCase for components]
- [Project-specific conventions]

**`tests/`** - Test suites
- Framework: [e.g., Jest, pytest, Go testing]
- Coverage: >80% for new features
- TDD required: Write tests before implementation (RED → GREEN → REFACTOR)

**`docs/`** - Documentation (Markdown format, keep in sync with code)

## Shared Standards

See `AGENTS.md` for:
- Code style and formatting
- Commit conventions (Conventional Commits)
- Quality gates and review process
- Prohibited patterns (no TODO, FIXME, @ts-ignore in new code)

## Thor Validation

Run before committing:
```bash
thor validate [file-path]
git diff --staged --name-only | xargs -I {} thor validate {}
```

Enforces: code style, test coverage, documentation, security best practices

## Model Preferences

- **Default:** `gpt-5.3-codex` (complex refactoring)
- **Fast:** `gpt-5.1-codex-mini` (quick fixes)
- **Complex logic:** `claude-opus-4.6` (architecture)
- **Documentation:** `claude-sonnet-4.5` (explanations)

## Project Conventions

**Code Style**
- Language: English for all code/comments
- Indentation: [e.g., 2 spaces]
- Line length: [e.g., 100 chars]
- Imports: [e.g., absolute paths, grouped by source]

**Error Handling:** [Your patterns]  
**Logging:** [Your conventions]  
**Dependencies:** [Guidelines for adding new deps]

## Build & Test Commands

```bash
# Install: [npm install | pip install -r requirements.txt | go mod download]
# Test:    [npm test | pytest | go test ./...]
# Build:   [npm run build | python setup.py build | go build]
# Lint:    [npm run lint | pylint | golangci-lint run]
# Format:  [npm run format | black . | gofmt -w .]
```

## Usage

1. Copy: `cp templates/.github-copilot-instructions-template.md .github/copilot-instructions.md`
2. Replace [bracketed] placeholders with project specifics
3. Remove irrelevant sections, add project-specific guidelines
4. Keep concise—link to detailed docs where needed
5. Commit and update as project evolves
