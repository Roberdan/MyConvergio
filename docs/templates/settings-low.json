{
  "$schema": "https://json.schemastore.org/claude-code-settings.json",
  "description": "LOW-TIER: For systems with 8GB RAM, 4 cores (MacBook Air, entry laptops)",
  "env": {
    "CLAUDE_CODE_MAX_OUTPUT_TOKENS": "32000",
    "MAX_THINKING_TOKENS": "16000",
    "BASH_DEFAULT_TIMEOUT_MS": "60000",
    "CLAUDE_CODE_DISABLE_NONESSENTIAL_TRAFFIC": "1",
    "NODE_OPTIONS": "--max-old-space-size=4096",
    "UV_THREADPOOL_SIZE": "4"
  },
  "permissions": {
    "defaultMode": "bypassPermissions"
  },
  "recommended_profile": "minimal",
  "notes": [
    "Use minimal profile: myconvergio install --minimal (8 agents)",
    "Keep only essential agents to minimize memory usage",
    "Consider using lean variants for further optimization"
  ]
}
