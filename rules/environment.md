# Environment Preferences

## Shell Aliases
- `cat` is aliased to `bat --paging=never` — do NOT pass `-l` flag (causes error in bat)
- Use `Read` tool for file reading instead of `cat` via Bash whenever possible
