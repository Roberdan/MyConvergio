async function renderEventFeed(){const el=$("#event-feed-content");if(!el)return;try{const events=await fetchJson("/api/events");if(!events||!events.length){el.innerHTML='<span style="color:var(--text-dim);font-size:11px">No events yet</span>';return}const icons={plan_completed:"✓",wave_completed:"◈",human_needed:"⚠",node_offline:"✗",task_status_changed:"◉"},colors={plan_completed:"var(--green)",wave_completed:"var(--cyan)",human_needed:"var(--gold)",node_offline:"var(--red)",task_status_changed:"var(--text-dim)"};el.innerHTML=events.slice(0,15).map(e=>{const icon=icons[e.event_type]||"•",color=colors[e.event_type]||"var(--text-dim)",age=e.created_at?_timeAgo(e.created_at):"",click=e.plan_id?` onclick="location.hash='#plan/${e.plan_id}'"`:"";return`<div class="event-row"${click} style="cursor:${e.plan_id?"pointer":"default"}"><span style="color:${color};font-weight:600;width:16px">${icon}</span><span class="event-type">${esc(e.event_type.replace(/_/g," "))}</span><span class="event-peer">${esc(e.source_peer||"")}</span>${e.plan_id?`<span class="event-plan">#${e.plan_id}</span>`:""}<span class="event-age">${age}</span></div>`}).join("")}catch{el.innerHTML='<span style="color:var(--text-dim)">Error loading events</span>'}}
function _timeAgo(ts){const d=Math.floor(Date.now()/1000)-ts;if(d<60)return d+"s";if(d<3600)return Math.floor(d/60)+"m";if(d<86400)return Math.floor(d/3600)+"h";return Math.floor(d/86400)+"d"}
function renderHistory(plans){$("#history-list").innerHTML=(plans||[]).map(p=>`<div class="history-row" onclick="openPlanSidebar(${p.id})"><span style="color:#00e5ff;font-weight:600">#${p.id}</span><span>${esc((p.name||"?").substring(0,28))}</span><span>${p.tasks_done}/${p.tasks_total}</span><span style="color:${p.status==="done"?"#00ff88":"#ff3355"}">${p.status==="done"?"✓":"✗"} ${p.status}</span></div>`).join("")}
window.openPlanSidebar=async function(planId){const data=await fetchJson(`/api/plan/${planId}`);if(!data||!data.plan)return;const p=data.plan,pct=p.tasks_total>0?Math.round(100*p.tasks_done/p.tasks_total):0,sColor=statusColor(p.status),lines=(p.lines_added||0)+(p.lines_removed||0)>0?`<br>Lines: <span style="color:var(--green)">+${p.lines_added||0}</span> / <span style="color:var(--red)">-${p.lines_removed||0}</span>`:"",cost=data.cost||{},costLine=cost.tokens||cost.cost?`<br>Tokens: ${fmt(cost.tokens)} · Cost: $${Number(cost.cost||0).toFixed(2)}`:"";$("#sb-title").textContent=`#${p.id} ${(p.name||"").substring(0,30)}`;let html=`<div class="sb-meta"><strong>Status:</strong> ${statusDot(p.status)} <span style="color:${sColor}">${p.status.toUpperCase()}</span><br><strong>Progress:</strong> ${p.tasks_done}/${p.tasks_total} (${pct}%)<br><strong>Host:</strong> ${esc(_resolveHost(p.execution_host))}${p.parallel_mode?`<br><strong>Mode:</strong> ${esc(p.parallel_mode)}`:""}${p.started_at?`<br><strong>Started:</strong> ${p.started_at}`:""}${p.completed_at?`<br><strong>Completed:</strong> ${p.completed_at}`:""}${lines}${costLine}</div>`;if(p.human_summary)html+=`<div class="sb-summary">${esc(p.human_summary)}</div>`;if(data.waves&&data.waves.length){html+='<div class="sb-section">Waves</div>';data.waves.forEach(w=>{const wp=w.tasks_total>0?Math.round(100*w.tasks_done/w.tasks_total):0,wColor=w.status==="done"?"var(--green)":w.status==="in_progress"?"var(--gold)":w.status==="cancelled"?"var(--red)":"var(--text-dim)";html+=`<div class="sb-wave"><div><span style="color:var(--cyan)">${esc(w.wave_id)}</span> <span style="color:var(--text-dim)">${esc((w.name||"").substring(0,20))}</span> <span style="color:${wColor};font-size:10px">${w.status}</span> ${thorIcon(w.validated_at)} ${w.pr_number?`<span style="color:var(--magenta);font-size:10px">PR #${w.pr_number}</span>`:""}<div class="sb-wave-bar"><div class="sb-wave-fill" style="width:${wp}%;background:${wColor}"></div></div></div><div style="text-align:right;color:var(--cyan);font-weight:600;font-size:11px">${wp}%</div></div>`})}
if(data.tasks&&data.tasks.length){html+='<div class="sb-section">Tasks</div>';data.tasks.forEach(t=>html+=`<div class="sb-task"><span class="sb-task-id">${esc(t.task_id)}</span><span>${esc((t.title||"—").substring(0,40))}</span><span style="color:${statusColor(t.status)}">${statusDot(t.status)} ${t.status}</span>${thorIcon(t.validated_at)}</div>`)}
$("#sb-body").innerHTML=html;$("#sidebar-overlay").classList.add("open");$("#sidebar").classList.add("open")};
window.closeSidebar=()=>{$("#sidebar-overlay").classList.remove("open");$("#sidebar").classList.remove("open")};
window.renderEventFeed=renderEventFeed;window.renderHistory=renderHistory;
